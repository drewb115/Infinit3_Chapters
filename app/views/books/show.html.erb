<!DOCTYPE html>
<html>
<head>
  <title>Books#show</title>
  <!-- Link to your CSS file -->
  <link rel="stylesheet" href="assets/stylesheets/book_details.css">
  <style>
    /* app/assets/stylesheets/book_details.css */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
      background-color: #f9f9f9;
      color: black; /* Set text color to black */
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }

    .title {
      text-align: center;
      font-size: 36px;
      color: #333;
      margin-bottom: 20px;
    }

    .details-box {
      background-color: #fff;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
      color: black; /* Set text color to black */
    }

    .book-details p {
      margin: 10px 0;
    }

    .btn {
      display: inline-block;
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      text-decoration: none;
      border: none;
      border-radius: 5px;
    }

    .btn:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1 class="title">Book Details</h1>

    <!-- Box for book details -->
    <div class="details-box">
      <div class="book-details">
        <p><strong>Title:</strong> <%= @book.title %></p>
        <p><strong>Author:</strong> <%= @book.author %></p>
        <p><strong>Description:</strong> <%= @book.description %></p>
        <p><strong>Genre:</strong> <%= @book.genre %></p>
        <p><strong>Price:</strong> <%= number_to_currency(@book.price) %></p>
        <!-- Add more details as needed -->

        <!-- Book Image -->
        <% if @book.image.present? %>
          <%= image_tag @book.image, style: "width: 300px; height: 300px;" %>
        <% else %>
          <!-- Placeholder image or alternative content if image is not present -->
          <p>No Image Available</p>
        <% end %>
      </div>
    </div>

    <!-- Home button -->
    <%= link_to "Home", root_path, class: "btn btn-primary" %>
  </div>

</body>
</html>
